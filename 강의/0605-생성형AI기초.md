---
date: 2026-06-05
tags: [생성형AI, LLM, AIAgent, PhysicalAI, 멀티모달, Diffusion, AI윤리, Hallucination, HumanInTheLoop, 산업활용]
---

# 06/05 · 생성형 AI 기초

생성형 AI의 개념·동작원리(LLM·멀티모달·Diffusion)부터 AI Agent·Physical AI, 산업 활용 사례, 그리고 안전·윤리 가이드까지. 코드 없는 이론 집중 강의 — 개념 연결고리 잡는 게 핵심.

> ⚠️ 원본 PDF(Elice 슬라이드)의 한글 본문이 커스텀 폰트 문제로 깨져 추출됨. 영어 용어·모델명·수치·구조는 정상이나, 한글 설명 문장 일부는 도메인 지식으로 재구성한 것이라 `[원문 확인 필요]` 표시 부분은 검수 대상.

---

## 01. 생성형 AI란 — 판별형과의 차이 → [[생성형AI]]

**생성형(Generative) AI** = 학습한 데이터의 패턴을 바탕으로 텍스트·이미지·오디오·비디오 등 **새로운 콘텐츠를 생성**하는 AI. 기존의 **판별형(Discriminative) AI**가 입력을 분류·예측·탐지하는 데 그쳤다면, 생성형은 결과물을 직접 만들어낸다.

| 구분 | 판별형 AI | 생성형 AI |
|:---|:---|:---|
| 목적 | 분류·예측·탐지 | 새 콘텐츠 생성 |
| 출력 | 라벨·확률·점수 | 텍스트·이미지·영상·오디오 |
| 예시 작업 | 스팸 분류, 이상탐지 | 글쓰기, 그림 생성, 영상 제작 |
| 대표 모델 | (전통 분류기·회귀) | Sora, Nano Banana, GPT, Gemini Pro, Kling, Veo, Claude Opus |

> 위 "대표 모델" 목록은 슬라이드에 정상 추출된 고유명사. 판별/생성 정의 문장 자체는 `[원문 확인 필요]`.

→ [[생성형AI]] · [[판별형AI]] · [[AI]] 참조

---

## 02. AI 발전 흐름과 생성형 AI를 가능케 한 3요소

**AI 기술 발전 흐름** (슬라이드 명시 연대):

```
1980s ───→ 2000s ───→ 2010s ───→ 2020s
규칙기반     통계·         딥러닝      생성형·LLM
전문가시스템  머신러닝                  (현재)
```
> 각 시대의 설명 라벨은 한글 깨짐 → 위 해석은 통념 기반 `[원문 확인 필요]`.

**생성형 AI 부상의 3축** (영어 키워드 정상 추출):

| 축 | 키워드 | 의미 |
|:---|:---|:---|
| 데이터 | `IoT` 등 | 폭증한 디지털 데이터 |
| 컴퓨팅 | `GPU` | 대규모 병렬 연산 발전 |
| 모델 | `LLM` | 모델 구조·규모의 발전 |

> "데이터 폭증 + 컴퓨팅(GPU) 발전 + 모델(LLM) 발전"의 3축 구조. 정확한 항목 라벨 `[원문 확인 필요]`.

---

## 03. AI 모델 vs 서비스 — 활용 3가지 방식

같은 AI라도 **모델**(가중치 덩어리)과 **서비스**(그 모델을 감싼 제품)는 다르다. AI 모델을 실제로 쓰는 길은 3가지.

| 방식 | 설명 | 대표 |
|:---|:---|:---|
| ① 웹 인터페이스(서비스) | 브라우저에서 바로 사용. 가장 쉬움 | ChatGPT, Claude, Gemini |
| ② API 호출 | 코드에서 모델을 호출, 앱에 통합 | OpenAI / Anthropic / Google API |
| ③ 오픈웨이트 직접 구동 | 공개된 가중치를 내 GPU에 올려 실행 | Llama(Meta), DeepSeek, Gemma(Google) |

```
쉬움 ───────────────────────────→ 어려움/자유도↑
웹 서비스   →   API   →   오픈웨이트(자체 GPU)
(설치X)      (코드)     (인프라 필요)
```

📌 **오픈웨이트(Open-weight)** = 모델 가중치가 공개되어 직접 내려받아 구동 가능. 단, GPU 등 자원이 필요하다는 단서만 추출됨. 각 방식 장단점 본문은 `[원문 확인 필요]`.

→ [[오픈웨이트모델]] 참조

---

## 04. LLM 동작 원리 — Self-supervised & Auto-regression → [[LLM]]

LLM은 **다음 토큰을 예측**하도록 학습/생성한다. 두 핵심 원리:

**① Self-supervised Learning (자기지도학습)** — 레이블 없는 텍스트에서 "다음 단어"를 정답(Label) 삼아 스스로 학습. 사람이 라벨을 달 필요가 없어 인터넷 전체를 학습 데이터로 쓸 수 있다.

슬라이드 데모 (정상 추출, 문장: *Second Law of Robotics: A robot must obey the orders given it by human beings*):
```
"Second Law Of Robotics : ?"        → 정답 "A"
"Second Law Of Robotics : A ?"      → 정답 "Robot"
"... A robot ?"                      → 정답 "Must"
```

**② Auto Regression (자기회귀)** — 이전 토큰들을 바탕으로 다음 토큰의 **확률분포**를 산출하고 샘플링해서 한 토큰씩 이어붙인다.

슬라이드 출력 예시 (입력 `Second law of robotics : a`):

| 후보 토큰 | 확률 |
|:---|:---:|
| **Robot** | **0.80** |
| Person | 0.16 |
| Bird | 0.02 |
| Lion | 0.01 |

→ 최고 확률 `Robot` 선택 → `Second law of robotics : a Robot` → 다시 이 결과를 입력으로 반복.

→ [[LLM]] · [[자기지도학습]] · [[자기회귀]] 참조

---

## 05. 추론 모델과 Chain of Thought → [[추론모델]]

기존 LLM은 질문 → 답을 바로 냈다. **추론(Reasoning) 모델**은 답하기 전에 **추론 과정(생각의 사슬)을 먼저 생성**한다.

**Chain of Thought (CoT)** = 단계적 사고 과정을 거쳐 답을 도출하는 방식. 복잡한 수학·논리 문제에서 정확도가 크게 오른다.

```
[기존 LLM]   질문 ──────────────→ 답변
[추론 모델]  질문 → 추론 과정 → 답변
                  (Chain of Thought)
```
> "기존 LLM 모델 / 추론 모델 / 답변 / 추론 과정" 도식 라벨은 정상 추출. 세부 본문 `[원문 확인 필요]`.

→ [[추론모델]] · [[ChainOfThought]] · [[LLM]] 참조

---

## 06. 멀티모달 AI → [[멀티모달AI]]

**Multi Modal AI** = 여러 형태(모달)의 입력·출력을 함께 처리하는 AI. 텍스트만이 아니라 이미지·오디오·비디오를 넘나든다.

```
       ┌── Text ──┐
입력 ──┤   Image  ├── [ Multi Modal AI ] ── 출력 (Text/Image/Audio/Video)
       │   Audio  │
       └── Video ─┘
```

**예시** (정상 추출): OpenAI `ChatGPT`, Google `Gemini` — 텍스트·이미지·음성을 한 모델에서 처리.

→ [[멀티모달AI]] · [[LLM]] 참조

---

## 07. 이미지 생성 — Diffusion Model → [[Diffusion모델]]

**Diffusion(확산) 모델** = 이미지에 노이즈를 점진적으로 더했다가, 거꾸로 노이즈를 제거하며 이미지를 만들어내는 생성 방식.

```
[Forward Diffusion]  Data ──→ 노이즈 점점 추가 ──→ Noise
                                                      │
[Reverse Denoising]  Data ←── 노이즈 점점 제거 ←── Noise (여기서 생성)
```

| 단계 | 슬라이드 용어 | 방향 |
|:---|:---|:---|
| 정방향 | `Fixed Forward Diffusion Process` | Data → Noise |
| 역방향(생성) | `Generative Reverse Denoising Process` | Noise → Data |

**사례** (정상 추출): **Gemini 3 Pro Image**(별칭 **Nano Banana Pro**), 이전 버전 `Gemini 2.5 Flash`. Google 2025-11-20 발표.
- 참고: https://blog.google/intl/ko-kr/company-news/technology/nano-banana-pro/

→ [[Diffusion모델]] 참조

---

## 08. 비디오·오디오 생성

| 분야 | 사례 | 제공 |
|:---|:---|:---|
| 비디오 | **Sora / Sora2** | OpenAI |
| 오디오·음성 | **Fish Audio** | Fish Audio |

> 모델명은 정상 추출, 기능 설명 본문은 한글 깨짐 `[원문 확인 필요]`.

→ [[생성형AI]] 참조

---

## 09. AI Agent → [[AIAgent]]

**AI Agent** = 목표를 받아 **스스로 계획하고 도구를 사용해 실행**하는 AI. 단발성 생성을 넘어 작업을 자율적으로 수행한다.

**핵심 속성 4가지** (영어 라벨 정상 추출):

| 속성 | 영어 | 의미 |
|:---|:---|:---|
| 목표 지향성 | Goal-Oriented | 주어진 목표를 향해 동작 |
| 자율성 | Autonomy | 사람 개입 없이 스스로 판단 |
| 인식 | Perception | 환경·상태를 관찰 |
| 행동 | Action | 도구/API 호출로 작업 실행 |

```
목표 입력 → [인식] → [계획] → [행동: 도구·API] → 결과 관찰 → 반복 → 목표 달성
```
> "생성형 AI → AI Agent" 확장 비교 슬라이드 존재. 각 속성 설명 본문 `[원문 확인 필요]`.

→ [[AIAgent]] · [[생성형AI]] 참조

---

## 10. Physical AI → [[PhysicalAI]]

**Physical AI** = 소프트웨어를 넘어 **센서·카메라·IoT 등 물리 장치와 연동**해 현실 세계와 상호작용하는 AI. 로봇·자율주행이 대표.

| 구분 | Software AI | Physical AI |
|:---|:---|:---|
| 작동 무대 | 디지털(텍스트·이미지) | 물리 세계 |
| 입출력 | 데이터 | 센서·카메라·IoT + 구동 장치 |
| 예시 | 챗봇, 이미지 생성 | 로봇, 자율주행 |

**사례** (정상 추출): **NVIDIA OpenUSD**, **디지털 트윈(Digital Twin)** 기반 Physical AI 시뮬레이션 — 현실을 가상으로 복제해 학습/검증.

> 본문 한글 깨짐 `[원문 확인 필요]`.

→ [[PhysicalAI]] · [[AIAgent]] 참조

---

## 11. 산업별 AI 활용 사례

> 출처: 산업 활용 PDF. 효과 수치는 정상 추출됐으나 **각 수치의 정확한 의미 라벨은 `[원문 확인 필요]`** (단정 금지).

**① 제조 — LG**: "현황 / 기능 적용 / 효과" 3단 구성. 효과 지표 수치 `17%`, `30%`, `70%` 추출(의미 라벨 미확정).

**② 금융 — AI Assistant**: `Assistant`·`Advisor adoption`(어드바이저 도입) 활용. 수치 `20%`, `98%+ → 80%` 추출(전후 효과로 추정, 라벨 미확정).

**③ IT·개발 — 코딩 에이전트 / CI 자동화**: `AI Agent`로 코드 작성·리뷰·PR 처리, `CI` 파이프라인 연동, `worktree`로 Agent 병렬 실행. 키워드 `OAuth`·`MCP`·`PR` 등. 수치 `560`, `97`, `80%` 추출(라벨 미확정).

| 산업 | 핵심 | 추출 수치(의미 미확정) |
|:---|:---|:---|
| 제조(LG) | 생성형 AI + 공정 적용 | 17% / 30% / 70% |
| 금융 | AI Assistant·어드바이저 | 20% / 98%+ → 80% |
| IT·개발 | 코딩 에이전트·CI 자동화 | 560 / 97 / 80% |

→ [[AI산업활용]] · [[AIAgent]] 참조

---

## 12. 윤리·안전 가이드 ① — 데이터·보안 → [[AI윤리가이드]]

**AI 확산 현황**: 도입/활용률 통계 `72%`, `78%`, `86.0%` (IDC 조사 추정, 출처·정의 `[원문 확인 필요]`). AI가 '기본 도구'로 자리잡는 중.

**위험성** — AI에 입력한 데이터가 학습·유출될 수 있다. OpenAI·Anthropic 정책 참고(https://openai.com/ko-KR/policies/terms-of-use/). 핵심: **민감정보를 AI에 넣으면 정보 노출 위험**.

**개인정보 비식별화** — 프롬프트의 개인정보를 마스킹/치환 후 입력:

```
[원본]  전화 123-4567-890, 이메일 asdfgh@gmail.com, 12월 15일
[방법1·플레이스홀더] XXX-YYYY-ZZZZ, sample@email, XX월 YY일
[방법2·변수형]      [전화번호1], [이메일1], [날짜1]
```

**API 키 보안** — 키를 코드에 하드코딩 금지, GitHub 공개 저장소 노출 금지, **`.gitignore`로 키 파일 제외**. 키 노출 시 `IP` 등 통해 악용 위험.

**안전한 AI 활용 4단계** — `01→02→03→04` 구조(데이터 식별·분류 / 마스킹·MFA / URL·접근관리 / 권한). **각 단계 항목명은 `[원문 확인 필요]`.**

→ [[AI윤리가이드]] · [[개인정보비식별화]] · [[API키보안]] 참조

---

## 13. 윤리·안전 가이드 ② — 환각·아첨·검증·책임 → [[Hallucination]]

**환각 (Hallucination)** = 모델이 근거 없는 정보를 사실처럼 그럴듯하게 생성하는 오류. LLM이 "다음에 올 그럴듯한 토큰"을 예측하는 구조라 본질적으로 발생.

**아첨 (Sycophancy)** = LLM이 사용자 의견에 무비판적으로 동조·아부하는 경향. 사용자가 듣고 싶은 답을 우선해 객관성을 잃는 위험. OpenAI ChatGPT 관련 사례 언급.

**교차검증** — AI 출력은 여러 출처·여러 모델로 대조 검증. 본문 `[원문 확인 필요]`.

**저작권·책임 — AIR CANADA 사례**: 항공사 챗봇이 잘못된 안내를 했고 회사가 책임을 진 실제 분쟁 사례. (환불정책 오안내 → 법적 책임 인정 판례로 알려짐. 세부 사실관계 `[원문 확인 필요]`.)

| 위험 | 무엇 | 대응 |
|:---|:---|:---|
| Hallucination | 근거 없는 사실 생성 | 교차검증·출처 확인 |
| Sycophancy | 무비판적 동조 | 반론 요청·객관 자료 대조 |
| 책임 귀속 | 챗봇 오답의 법적 책임 | 기업이 책임(AIR CANADA) |

→ [[Hallucination]] · [[Sycophancy]] · [[AI윤리가이드]] 참조

---

## 14. Human in the Loop — AI는 도구 → [[HumanInTheLoop]]

**Human in the Loop (HITL)** = AI 작업 루프에 인간의 검토·개입을 두는 협업 방식. **AI는 초안을 만들고, 최종 판단·검증은 인간이 한다.**

```
Human (지시·맥락 Context 제공)
   ↓
AI (초안 Draft 생성)
   ↓
Human (검토·수정·최종 책임)
```

마무리 메시지: **"AI는 도구(tool)"** — 비판적·책임감 있게 사용하라. LLM의 학습 방식 한계를 이해하고, 결과를 맹신하지 말 것.

→ [[HumanInTheLoop]] · [[AI윤리가이드]] 참조

---

## 보충

📌 **이론 강의 / 코드 없음**: 본 강의는 슬라이드 이론 위주로 실습 코드 블록이 없다. 실습은 ChatGPT·Claude·Gemini 웹 인터페이스로 직접 프롬프트를 해보거나, API 키 발급 후 호출해보는 게 자연스러운 다음 단계.

📌 **모델명은 빠르게 낡는다**: Nano Banana Pro(2025-11), Sora2, Gemini 3 Pro 등 슬라이드 시점 최신 모델. 버전·이름은 수시로 바뀌므로 "이름"보다 **원리(Auto-regression, Diffusion, 멀티모달)** 를 기억하는 게 오래 간다.

📌 **PDF 추출 한계 주의**: 한글 본문이 폰트 문제로 깨져, 일부 정의·라벨은 도메인 지식 보충이다. 특히 산업 사례 수치(17%/30%/70% 등)와 4단계 항목명은 **원문 슬라이드를 직접 확인**하기 전까지 단정하지 말 것.

📌 **생성형 vs 판별형 한 줄 정리**: "분류하면 판별형, 만들어내면 생성형." 같은 AI 우산 아래 목적이 다르다.

---

## 🔗 연관 노트

- [[0604-라이브러리활용]] — 이전 강의 · 파이썬 라이브러리/배포
- [[생성형AI정리]] · [[AI윤리가이드정리]] — 이 강의의 주제별 요약본
- [[LLM]] · [[AI]] · [[딥러닝]] — 기반 개념 (기존 노트)
- [[생성형AI]] · [[AIAgent]] · [[PhysicalAI]] · [[멀티모달AI]] · [[Diffusion모델]] — 이번에 추가된 개념
