---
date: 2026-06-02
tags: [파이썬, 클래스, OOP, 객체지향, 상속, 다형성, 캡슐화, 매직메서드, classmethod, staticmethod, 모듈, 패키지, import, 변수, 스코프]
---

# 06/02 · 클래스(OOP) · 모듈과 패키지

객체지향(클래스)으로 "팀원·부서가 있는 IT 회사"를 만들고, 코드를 모듈·패키지로 나눠 `import`하는 법. 한 줄 요약: **데이터+행동을 클래스로 묶고(캡슐화) → 물려받고(상속) → 같은 명령에 다르게 반응(다형성), 그리고 코드를 파일·폴더로 나눠 가져다 쓰기(모듈·패키지).**

> 클래스에 들어가기 전, 함수에서 미뤄둔 **지역/전역 변수(스코프)** 를 먼저 짚었다(00번). 클래스 변수가 "공유되는 상태"라는 점과 이어지는 준비운동.

---

## 00. 지역 변수 / 전역 변수 (스코프)

변수가 **어디서 통하는지**가 스코프(scope). 클래스 변수(공유 상태)로 넘어가기 전에 "변수가 사는 범위"부터 정리했다.

| 구분 | 어디서 정의 | 어디서 접근 |
|:---|:---|:---|
| **지역 변수**(Local) | 함수 **안** | 그 함수 안에서만 |
| **전역 변수**(Global) | 함수 **밖** | 프로그램 어디서나 |

**① 지역 변수는 함수 밖에서 못 쓴다 → `NameError`**

```python
def greet():
    msg = "함수 안에서 안녕!"   # 지역 변수
    print(msg)

greet()          # 함수 안에서 안녕!
print(msg)       # NameError: name 'msg' is not defined  ← 밖에선 모름
```

**② 전역 변수는 함수 안/밖 모두 읽기 OK**

```python
msg = "Python is awesome!"   # 전역 변수

def display():
    print("함수 안:", msg)    # 읽기는 가능

display()                    # 함수 안: Python is awesome!
print("함수 밖:", msg)        # 함수 밖: Python is awesome!
```

**③ Shadowing — 같은 이름의 지역이 전역을 가린다**

```python
s = "I love yeardreamschool"

def fun():
    s = "Me too."    # 전역 s를 가리는 새 지역 s
    print(s)         # Me too.

fun()
print(s)             # I love yeardreamschool  ← 전역은 그대로
```

**④ 전역을 함수 안에서 수정하려면 `global` 선언**

그냥 대입하면 새 지역 변수가 생길 뿐 전역은 안 바뀐다. 진짜 바꾸려면 `global`.

```python
count = 0
def increase():
    global count         # "이 count는 전역이다" 선언
    count = count + 1

increase()
print(count)             # 1
increase()
print(count)             # 2   ← global 덕분에 누적된다
```

`global` 없이 매개변수 기본값으로 만들면 그 `count`는 **지역**이라 전역엔 영향이 없다(대조).

```python
count = 0
def increase(count=0):   # 이 count는 매개변수 = 지역
    count = count + 1
    return count

increase()        # 1
increase()        # 1   ← 매번 0에서 시작 (누적 X)
print(count)      # 0   ← 전역 count는 그대로
```

> 수업 의도: 한 번에 완벽히 이해하라는 게 아니라 **"헷갈린다"는 걸 인지하고 반복으로 익히라**는 것. 곧 나오는 클래스 변수(공유 상태)·`self`(객체 상태)를 이해하려면 "변수가 어디서 통하는가"가 깔려 있어야 한다.

→ 변수 범위 상세는 [[변수]], 함수 관점의 정리는 [[함수]] 참조

---

## 01. 클래스 = 설계도, 인스턴스 = 객체

**클래스**는 물건을 찍어내는 **설계도(붕어빵 틀)**, 그 틀로 만든 실제 물건 하나하나가 **인스턴스(객체)**.

| 구분 | 의미 | 예 |
|:---|:---|:---|
| 클래스 | 설계도 (틀) | `TeamMember` |
| 인스턴스 | 설계도로 만든 실제 객체 | `alice = TeamMember(...)` |
| 속성 | 객체가 가진 **데이터** | 이름·직책·연봉 |
| 메서드 | 객체가 하는 **행동** | 자기소개·연봉협상 |

```python
class TeamMember:        # 이름 첫 글자 대문자(관례)
    pass

a = TeamMember()
b = TeamMember()
print(a is b)            # False — 서로 다른 객체
```

```
      TeamMember          ← 클래스 (설계도 / 붕어빵 틀)
          │ TeamMember("Alice", 6000)   찍어내기
   ┌──────┼───────┐
 alice    bob    carol    ← 인스턴스 (실제 객체)
 각자 다른 이름·연봉(속성)을 가짐
```

→ [[클래스]] 참조

---

## 02. `__init__` 와 `self`

`__init__` = 객체가 **생성되는 순간 자동 실행**되는 생성자. `self` = **"이 객체 자신"**.

```python
class TeamMember:
    def __init__(self, name, role, salary):
        self.name = name        # self.속성 = 값 → 이 객체에 저장
        self.role = role
        self.salary = salary

alice = TeamMember("Alice", "시니어 개발자", 6000)
print(alice.name, alice.salary)   # 객체.속성 으로 꺼냄
```

---

## 03. 메서드 — 객체의 행동

클래스 안에 정의한 함수. 첫 인자는 항상 `self`(호출 시 자동 전달).

```python
class TeamMember:
    def __init__(self, name, salary):
        self.name, self.salary = name, salary
    def introduce(self):
        print(f"{self.name}입니다.")
    def negotiate(self, amount):
        self.salary += amount

alice = TeamMember("Alice", 3000)
alice.introduce()        # self = alice 자동
alice.negotiate(2000)    # salary → 5000
```

---

## 04. 클래스 변수 vs 인스턴스 변수

| 구분 | 값 | 위치 |
|:---|:---|:---|
| **인스턴스 변수** | 객체마다 **다름** (이름·연봉) | `__init__` 안 `self.x` |
| **클래스 변수** | 모든 객체가 **공유** (회사명·카운터) | 클래스 안, `__init__` 밖 |

```python
class TeamMember:
    company = "이어드림"        # 클래스 변수 (공유)
    total = 0
    def __init__(self, name):
        self.name = name        # 인스턴스 변수
        TeamMember.total += 1   # 새 객체마다 +1

TeamMember("A"); TeamMember("B")
print(TeamMember.total)         # 2
```

> 💡 **라이브 강조**: 클래스 변수(`company`)를 `TeamMember.company = "..."`로 바꾸면 **모든 인스턴스 출력에 즉시 반영**된다 — "공유 상태"라는 게 이 의미. 그리고 카운터 이름을 `total` ↔ `total_member`처럼 한 글자라도 틀리면 바로 에러 나니 **오타 주의**(실습 중 실제 발생 사례).

---

## 05. 매직(던더) 메서드 — `__이름__`

앞뒤 밑줄 2개. 파이썬이 **특정 순간 자동 호출**.

| 메서드 | 자동 호출 시점 |
|:---|:---|
| `__init__` | 객체 생성 |
| `__str__` | `print()` — 사람이 읽기 좋게 |
| `__repr__` | 디버깅·콘솔 표현 |
| `__len__` | `len()` |
| `__eq__` | `==` 비교 |

```python
class TeamMember:
    def __init__(self, name): self.name = name
    def __str__(self): return f"팀원 {self.name}"

print(TeamMember("Alice"))   # 팀원 Alice  (없으면 <...object at 0x..>)
```

---

## 06. classmethod vs staticmethod

| 종류 | 데코레이터 | 첫 인자 | 쓰임 |
|:---|:---|:---|:---|
| 인스턴스 메서드 | 없음 | `self` | 객체 데이터 사용 |
| **클래스 메서드** | `@classmethod` | `cls` | 클래스 변수·대체 생성자 |
| **정적 메서드** | `@staticmethod` | 없음 | 클래스 관련 독립 기능 |

```python
class TeamMember:
    total = 0
    @classmethod
    def get_total(cls): return cls.total      # cls = 클래스 자신
    @staticmethod
    def valid_salary(s): return 1000 <= s <= 10000   # self·cls 없음
```

> `@`는 **데코레이터** — 함수에 기능을 "표시/포장"하는 장치. → [[함수]]

> 📌 **대체 생성자 `from_string`**: 문자열 하나로 객체를 만드는 클래스 메서드. 수업 예제는 `"이름-직책-연봉"`처럼 **하이픈(`-`)** 으로 끊어 `split("-")` 후 `cls(...)`로 생성. (미니프로젝트 확장본에서는 콜론 `:` 구분자를 쓰지만, 본 수업 기준 표기는 하이픈으로 통일.)

---

## 07. 상속 (Inheritance)

부모 클래스의 속성·메서드를 **물려받아** 자식 클래스를 만듦. 공통은 부모에, 차이만 자식에.

```python
class Department:                  # 부모: 공통
    def __init__(self, team):
        self.team = team
        self.members = []

class DevTeam(Department):         # 자식: 상속
    def __init__(self, stack):
        super().__init__("개발팀")    # 부모 __init__ 호출
        self.stack = stack          # 자식만의 속성

dev = DevTeam(["Python"])
print(dev.team)                    # 개발팀 (물려받음)
```

```
          Department         ← 부모 (공통: team·members·meeting)
              ▲ 상속 super()
       ┌──────┴───────┐
    DevTeam        SalesTeam   ← 자식 (meeting() 각자 재정의 = 오버라이딩)
   "스프린트 회의"  "매출 회의"   ← 같은 호출, 다른 결과 = 다형성
```

> `super()` = 부모 클래스. `super().__init__(...)`로 부모 초기화 그대로 실행.
> 💡 라이브 강조: 상속은 **문법보다 "설계(계층 구조)" 이해가 핵심.** 공통을 부모로 올리고 차이만 자식에 두는 감각.

→ [[상속]] 참조

---

## 08. 오버라이딩 · 다형성

- **오버라이딩**: 부모 메서드를 자식이 **자기 방식으로 다시 정의**
- **다형성**: **같은 메서드 호출**인데 객체마다 **다르게 동작**

```python
class Department:
    def meeting(self): print("회의")
class DevTeam(Department):
    def meeting(self): print("개발팀: 스프린트 회의")
class SalesTeam(Department):
    def meeting(self): print("영업팀: 매출 회의")

for t in [DevTeam(), SalesTeam()]:   # 같은 호출, 다른 결과
    t.meeting()
```

---

## 09. 모듈 — `import` 4가지

**모듈 = 파이썬 코드가 담긴 `.py` 파일.** 잘 만든 코드를 `import`로 가져다 씀.

| 방법 | 코드 | 사용 |
|:---|:---|:---|
| 통째로 | `import math` | `math.sqrt(9)` |
| 별칭 | `import random as rd` | `rd.choice(...)` |
| 콕 집어 | `from math import sqrt, pi` | `sqrt(9)` |
| 전부(`*`) | `from math import *` | 비권장 (이름 충돌) |

```python
import math
print(math.pi, math.sqrt(16))     # 3.141.. 4.0
```

→ [[모듈과패키지]] 참조

---

## 10. 패키지 — 모듈을 폴더로 묶기

모듈(`.py`)이 많아지면 폴더로 분류. 그 폴더가 **패키지**. 점(`.`)으로 경로를 따라감.

```
project/
├── script/main.py
└── sound/
    ├── audio.py
    └── play.py
```

```python
import sound.audio               # sound 폴더의 audio 모듈
from sound.play import start      # sound/play.py 의 start
```

> 정리: **모듈 = 파일(.py)**, **패키지 = 모듈 담은 폴더**.

---

## 보충

📌 **`if __name__ == "__main__":`** — 모듈을 **직접 실행할 때만** 동작, `import`되면 안 돌아감. 모듈 안 테스트 코드용. (실무문법에서 다시 등장 → [[0604-실무문법패턴]])

📌 **모듈 분리의 실제 예 (미니프로젝트)**: 수업 미니프로젝트는 `department.py`(클래스 정의) → `company.py`(`from department import ...`) → `main.py`(`from company import Company`, 엔트리포인트 `if __name__ == "__main__":`)의 **3파일 import 구조**. 클래스(상속·다형성) + 모듈/패키지를 한데 묶은 종합 예제다.

📌 **isinstance() / issubclass()** *(예습 — 수업 미진행, 배운 뒤 보강)*: 객체 타입·상속 관계 확인. `isinstance(dev, Department)` → True(부모 타입도 True), `issubclass(DevTeam, Department)` → True.

📌 **캡슐화 — `_`, `__` 관례**: `_name`="직접 쓰지 마세요"(약속), `__name`=이름 맹글링(name mangling)으로 외부 접근 차단. 파이썬은 강제 private 없이 **관례**로 표현.

📌 **표준 모듈 ≠ 외부 패키지**: 표준 라이브러리(math·random)는 설치 없이 `import`, 외부(numpy·pandas)는 `pip`/`uv`로 설치 후 사용. → [[0604-라이브러리활용]]

---

## 🔗 연관 노트

- [[0601-파이썬기초2]] — 이전 강의 · 함수·자료구조 (클래스의 전제)
- [[0604-실무문법패턴]] — 다음 강의 · 실무 심화 문법
- [[객체지향-클래스]] — 클래스·상속·모듈 요약 정리본
- [[클래스]] · [[상속]] · [[모듈과패키지]] · [[함수]] · [[변수]] — 개념 상세
